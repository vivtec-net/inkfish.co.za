---
interface Props {
	href: string;
	class?: string;
	[key: string]: any;
}

const { href, class: className, ...rest } = Astro.props;
const finalUrl = `${import.meta.env.BASE_URL}${href}`;
---

<a href={finalUrl} class={className} {...rest}>
	<slot />
</a>
